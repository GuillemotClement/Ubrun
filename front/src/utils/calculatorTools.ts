export const calculatorTools = {
  /**
   * calculate % from a value
   *
   * @param {number} value
   * @param {number} purcent
   * @returns {number}
   */
  getPurcentValue(value: number, purcent: number) {
    return value * (purcent / 100);
  },
};